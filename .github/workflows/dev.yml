name: Deploy Dev

on:
  workflows: ["Rentez CI"]
    types:
      - completed
    branches:
      - dev

  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: dev
