# Build output — the image builds its own, and copying a host-built target/
# in would break the layer cache and risk shipping stale classes.
target/

# IDE + VCS
.idea/
*.iml
.git/
.gitignore

# Local tooling
.mvn/wrapper/maven-wrapper.jar
*.log
