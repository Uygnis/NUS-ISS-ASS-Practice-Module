// Runtime configuration for RentEz frontend.
// Override these by mounting your own config.js (Docker) or editing the
// service URLs from the in-app settings panel (stored per-browser).
window.__RENTEZ_GATEWAY_ENDPOINT__ = "http://localhost:8080";
